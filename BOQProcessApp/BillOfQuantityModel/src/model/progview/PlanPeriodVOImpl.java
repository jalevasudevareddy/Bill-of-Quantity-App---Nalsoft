package model.progview;

import java.sql.Date;
import java.sql.ResultSet;

import java.util.ArrayList;
import java.util.List;

import java.util.ListIterator;

import oracle.jbo.server.ViewObjectImpl;
import oracle.jbo.server.ViewRowImpl;
import oracle.jbo.server.ViewRowSetImpl;
// ---------------------------------------------------------------------
// ---    File generated by Oracle ADF Business Components Design Time.
// ---    Thu Jan 19 12:10:54 IST 2017
// ---    Custom code may be added to this class.
// ---    Warning: Do not modify method signatures of generated methods.
// ---------------------------------------------------------------------
public class PlanPeriodVOImpl extends ViewObjectImpl {
    /**
     * This is the default constructor (do not remove).
     */
    public PlanPeriodVOImpl() {
    }

    /**
     * executeQueryForCollection - overridden for custom java data source support.
     */
    protected void executeQueryForCollection(Object qc, Object[] params,
                                             int noUserParams) {
        try {
            if (params != null) {
                Object[] o1 = (Object[])params[0];
                Object startDate = o1[1];
                Object[] o2 = (Object[])params[1];
                Object endDate = o2[1];
                if (startDate != null) {
                    String[] startDateArr = startDate.toString().split("-");
                    String[] endDateArr =
                        endDate != null ? endDate.toString().split("-") : null;
                    int startYear = Integer.parseInt(startDateArr[0]);
                    int endYear =
                        endDateArr != null ? Integer.parseInt(endDateArr[0]) :
                        startYear + 10;
                    List<String> yearList = new ArrayList<String>();
                    while (startYear <= endYear) {
                        int x = (startYear);
                        String p1 = x + " -";
                        if (x + 3 < endYear) {
                            x = x + 3;
                        } else {
                            x = endYear;
                        }
                        String p2 = p1 + " " + x;
                        yearList.add(p2);
                        startYear = x + 1;
                        //            System.out.println(yearList);
                    }

                    if (!yearList.isEmpty()) {
                        ListIterator<String> agrYearItr =
                            yearList.listIterator();
                        setUserDataForCollection(qc, agrYearItr);
                        super.executeQueryForCollection(qc, params,
                                                        noUserParams);
                    }

                }
            }
        } catch (Exception e) {
            e.printStackTrace();
        }
    }

    /**
     * hasNextForCollection - overridden for custom java data source support.
     */
    protected boolean hasNextForCollection(Object qc) {
        boolean result = false;

        ListIterator<String> agrListItr =
            (ListIterator<String>)getUserDataForCollection(qc);
        result = agrListItr.hasNext();
        if (!result) {
            setFetchCompleteForCollection(qc, true);
        }
        return result;
    }

    /**
     * createRowFromResultSet - overridden for custom java data source support.
     */
    protected ViewRowImpl createRowFromResultSet(Object qc,
                                                 ResultSet resultSet) {
        ListIterator<String> agrListItr =
            (ListIterator<String>)getUserDataForCollection(qc);

        ViewRowImpl row = createNewRowForCollection(qc);
        try {
            String planPeriod = agrListItr.next();
            row.setAttribute("PlanPeriod", planPeriod);

        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        return row;
    }

    /**
     * getQueryHitCount - overridden for custom java data source support.
     */
    public long getQueryHitCount(ViewRowSetImpl viewRowSet) {
        long value = super.getQueryHitCount(viewRowSet);
        return value;
    }

    /**
     * Returns the bind variable value for p_start_date.
     * @return bind variable value for p_start_date
     */
    public Date getp_start_date() {
        return (Date)getNamedWhereClauseParam("p_start_date");
    }

    /**
     * Sets <code>value</code> for bind variable p_start_date.
     * @param value value to bind as p_start_date
     */
    public void setp_start_date(Date value) {
        setNamedWhereClauseParam("p_start_date", value);
    }

    /**
     * Returns the bind variable value for p_end_date.
     * @return bind variable value for p_end_date
     */
    public Date getp_end_date() {
        return (Date)getNamedWhereClauseParam("p_end_date");
    }

    /**
     * Sets <code>value</code> for bind variable p_end_date.
     * @param value value to bind as p_end_date
     */
    public void setp_end_date(Date value) {
        setNamedWhereClauseParam("p_end_date", value);
    }
}
